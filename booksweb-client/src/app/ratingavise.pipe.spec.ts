import { RatingavisePipe } from './ratingavise.pipe';

describe('RatingavisePipe', () => {
  it('create an instance', () => {
    const pipe = new RatingavisePipe();
    expect(pipe).toBeTruthy();
  });
});
